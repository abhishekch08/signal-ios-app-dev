{
  "name": "signal-personal-newspaper",
  "version": "1.0.0",
  "private": true,
  "description": "A fast, credible-source-only personal newspaper.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
